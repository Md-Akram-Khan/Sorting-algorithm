void insertion sort(vector<int> &a){
    int n = a.size();
    for (int i = 1; i < n; i++) 
    {
        int j = i;
        while (j - 1 >= 0 && a[j - 1] > a[j]) 
        {
            swap(a[j], a[j - 1]);
            j--;
        }
    }
}
